import React from 'react';

const Excellences = () => {
    const excellenceImages = () => {
        const images = [

        ]
    }
    return (
        <div>

        </div>
    );
};

export default Excellences;